import axios from "axios";

// export const apiService = axios.create({
//   baseURL: "/api",
//   timeout: 30000,
// });

export const apiService = axios.create({
  baseURL: "/api",
  timeout: 30000,
});
